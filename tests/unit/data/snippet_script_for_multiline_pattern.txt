"""Python test script is generated by Regexapp Community Edition

Created by  : user1
Email       : user1@abcxyz.com
Company     : ABC XYZ LLC
Created date: 2021-08-14
"""

import re

# test data
test_data = """I live in Abc Xyz.
My city is friendly.
You will meet a lot of nice people.
People enjoy fishing during weekend."""

# regex pattern
pattern = r"(?s)(?P<subject1>\w+( +\w+)*) +live +in +(?P<object1>\w+( +\w+)*)\.[^\r\n]*[\r\n]+([^\r\n]*[\r\n]+)*(?P<subject2>\w+( +\w+)*) +will +meet +(?P<object2>\w+( +\w+)*)\."

def test_regex(test_data, pattern):
    """test regular expression

    Parameters
    ----------
    test_data (str): a test data
    pattern (str): a regular expression pattern
    """

    print("Pattern: {}".format(pattern))
    match = re.search(pattern, test_data)
    if match:
        if match.groupdict():
            print("Result: {}".format(match.groupdict()))
        else:
            print("Result: {}".format(match.group()))
    else:
        print("Result: No match")

# function call
test_regex(test_data, pattern)